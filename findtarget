class Solution(object):
    def targetIndices(self, nums, target):
        holder=[]
        nums=sorted(nums)
        for i in range(len(nums)):
            if nums[i]==target:
                holder.append(i)
        return holder
